"""
Haven't started this bit yet. This is the "try to break my own model" part.

A lot of student projects just train a model, get a good accuracy number, and stop there. I don't
want to do that, I want to actually check if the model still works when someone's trying to trick
it.

The idea:
    - Make a small test set by hand: typos, homoglyphs (characters that look the same as letters
      but technically aren't, like a zero instead of a capital O), extra random spaces, and
      rewording the urgent/scary language a bit.
    - Run my best model against each of those and see how much worse it does.
    - Write up what actually broke it and why, and try to fix it if I can.

Needs src/combine_model.py done first.
"""

raise NotImplementedError("haven't built this bit yet, see the plan in README.md")
