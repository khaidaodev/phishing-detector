"""
Haven't started this bit yet, this is where I combine the text model and the link model.

The idea:
    - Option A: stick the text model's output and the link model's output together into one
      bigger model.
    - Option B: "stacking", train a small model on top of what the other two models predict.
    - Try both, compare the numbers against the two separate models, keep whichever wins.

Needs src/url_features.py done first.
"""

raise NotImplementedError("haven't built this bit yet, see the plan in README.md")
